{% extends 'base.html.twig' %}

{% block title %}Changement mot de passe{% endblock %}

{% block body %}
    <br/><br/><br/><br/>

    <div class="d-flex justify-content-center border border-info form-css">
        <h3 class="h3 mb-3 font-weight-normal">Changer votre mot de passe ici</h3>

    {{ form_start(changePasswordForm) }}
        {{ form_row(changePasswordForm.plainPassword) }}

        <button class="btn badge badge-info" type="submit">réinitialiser le mot de passe</button>
    {{ form_end(changePasswordForm) }}
    </div>
{% endblock %}
